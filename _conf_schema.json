{
  "github_token": {
    "description": "GitHub Personal Access Token",
    "type": "string",
    "default": "",
    "hint": "需要 repo 权限的 token，在 GitHub Settings → Developer settings → Personal access tokens 生成"
  },
  "trigger_keyword": {
    "description": "触发创建 issue 的关键字",
    "type": "string",
    "default": "issue"
  },
  "require_at_bot": {
    "description": "是否要求 @机器人 才能触发",
    "type": "bool",
    "default": true
  },
  "http_proxy": {
    "description": "HTTP proxy",
    "type": "string",
    "default": "",
    "hint": "e.g. http://127.0.0.1:7890"
  },
  "llm_system_prompt": {
    "description": "LLM 系统提示词",
    "type": "text",
    "default": "你是一个专业的 GitHub Issue 整理助手，负责将聊天记录中的问题或需求整理为结构清晰的 Issue 内容。",
    "hint": "自定义 LLM 整理 issue 时使用的系统提示词。留空使用默认值"
  }
}
